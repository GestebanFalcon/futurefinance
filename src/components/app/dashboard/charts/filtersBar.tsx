"use client"
import { Dispatch, SetStateAction, useCallback, useEffect, useState } from "react";
import { Filters } from "./chartSpace";

export default function FiltersBar({ setFilters, filters }: { filters: Filters, setFilters: Dispatch<SetStateAction<Filters>> }) {

    return (
        <div className="flex flex-row bg-neutral-200">



        </div>
    )
}