"use server"

import { SelectBankAccount } from "@/lib/drizzy/schema/other";

export default function editBankAccount({ id, name, type }: { id: SelectBankAccount["id"], name: string, type: string }) {



}